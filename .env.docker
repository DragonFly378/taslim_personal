# Docker Environment Configuration for Taslim
# Copy this file to .env when running with Docker Compose

# Database (Internal - accessed via Docker network)
# The database URL points to the 'db' service name, not localhost
DATABASE_URL="mysql://taslim:taslim123@db:3306/taslim"

# NextAuth Configuration
# Update NEXTAUTH_URL to your production domain when deploying
NEXTAUTH_URL="http://localhost:3000"

# Generate a secure secret with: openssl rand -base64 32
NEXTAUTH_SECRET="change-this-to-a-random-secret-key-generate-with-openssl"

# OAuth Providers (Optional)
# Add your OAuth credentials here if you want to enable social login
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# App Config
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# MySQL Database Configuration (used by docker-compose.yml)
MYSQL_ROOT_PASSWORD="password"
MYSQL_DATABASE="taslim"
MYSQL_USER="taslim"
MYSQL_PASSWORD="taslim123"

# Notes:
# - Database is only accessible within Docker network (not exposed to host)
# - Only the web application is exposed on port 3000
# - Adminer (database UI) is available internally but not exposed by default
#   Uncomment the ports section in docker-compose.yml to expose Adminer on port 8080
